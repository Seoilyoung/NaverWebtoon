/*
package gabriel.com.nwebtoon_android.adapter;

import android.view.View;
import android.view.ViewGroup;
import android.widget.BaseAdapter;
import android.widget.ImageView;
import android.widget.TextView;

import com.bumptech.glide.Glide;

import java.sql.Date;
import java.text.SimpleDateFormat;
import java.util.List;

import gabriel.com.nwebtoon_android.R;
import gabriel.com.nwebtoon_android.model.webtoonListDay;

*/
//일영이가 맨처음에 메인 페이지에서 쓸려고 만들어놓은 어뎁터
/*


public class webtooonListDayAdapter extends BaseAdapter {
    //SimpleDateFormat dateformat = new SimpleDateFormat("yyyy년 MM월 dd일 hh시 mm분 ss초");
    SimpleDateFormat dateformat = new SimpleDateFormat("yyyy년 MM월 dd일");
    private List<webtoonListDay> webtoonListDays;

    public void append(webtoonListDay webtoonListDay) {

        this.webtoonListDays.add(0, webtoonListDay);
        this.notifyDataSetChanged();
    }

    public void setSource(List<webtoonListDay> webtoonListDays) {

        this.webtoonListDays = webtoonListDays;
        this.notifyDataSetChanged();
    }

    @Override
    public int getCount() { return webtoonListDays != null ? webtoonListDays.size() : 0; }

    @Override
    public Object getItem(int position) { return (webtoonListDays != null && (0 <= position && position < webtoonListDays.size())) ? webtoonListDays.get(position) : null; }

    @Override
    public long getItemId(int position) { return 0; }

    @Override
    public View getView(int position, View view, ViewGroup parent) {

        view = View.inflate(parent.getContext(), R.layout.list_webtoon_my_current, null);

        webtoonListDay webtoonListDay = webtoonListDays.get(position);

        ImageView img_thumbnail = (ImageView)view.findViewById(R.id.img_thumbnail);
        Glide.with(view.getContext()).load(webtoonListDay.TOON_THM).thumbnail(0.1f).into(img_thumbnail);

        TextView txt_list_name = (TextView) view.findViewById(R.id.txt_list_name);
        txt_list_name.setText(webtoonListDay.TOON_NAME);

        TextView txt_list_wtr = (TextView) view.findViewById(R.id.txt_list_wtr);
        txt_list_wtr.setText(webtoonListDay.TOON_WTR);

        TextView txt_list_update_dt = (TextView) view.findViewById(R.id.txt_list_update_dt);
        Date date = new Date(webtoonListDay.UPDATE_DT);
        txt_list_update_dt.setText(dateformat.format(date));

        return view;
    }

}
*/
